

// Addendum 

// ===Normal While Loop===

const movies = ['Barbie', 'Arrival', 'Get Out', 'Coco']

movies.forEach(function(movie) {
  console.log(movie)
})

// ===Simple While Loop===

const array1 = ['a', 'b', 'c']

for (element of array1) {
  console.log(element)
} 